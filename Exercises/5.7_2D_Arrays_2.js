// Video 5.1 accompanies this exercise

// Now alert a string with the full title of the first movie in the
// eightiesMovies array, but only using the eightiesMovies variable to access
// the correct values. Use the concatenation operator to unite the words into
// one string, and remember to be attentive to necessary whitespace!

// var movie1 = [16, "Candles"];
// var movie2 = [3, "Men", "and", "a", "Baby"];
// var eightiesMovies = [movie1, movie2];
//
// // const litle = eightiesMovies[1][0] + " " + eightiesMovies[1][1] + " " + eightiesMovies[1][2] + " " + eightiesMovies[1][3] + " " + eightiesMovies[1][4]
// //
// // console.log(litle);
//
// const litle = `${eightiesMovies[1][0]} ${eightiesMovies[1][1]} ${eightiesMovies[1][2]} ${eightiesMovies[0][3]} ${eightiesMovies[0][4]}`
//
// console.log(litle);

const students = ['betty', 'jorge', 'floo-floo', 'frankie']

for(let i = 0; i < students.length; i++)
console.log(i, students[i]);
